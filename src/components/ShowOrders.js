// src/components/ShowOrders.js
import React from 'react';

const ShowOrders = ({ fetchData }) => {
  return (
    <div>
      <h4>Show Orders Component</h4>
      {/* Add content or functionality for ShowOrders here */}
    </div>
  );
};

export default ShowOrders;
